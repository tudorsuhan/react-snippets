import React, { Component } from 'react';
import './HugeText.js';

export default class HugeText extends Component {
    render() {
        return (
            <div className="huge-text contact-text">Contact</div>
        );
    }
}
